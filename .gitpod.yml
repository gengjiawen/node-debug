image: gitpod/workspace-wasm:latest

tasks:
  - init: npm i -g @gengjiawen/node-dev && node-dev setuplldb && node-gyp --debug configure rebuild

vscode:
  extensions:
    - vadimcn.vscode-lldb

# lldb init
# command script import /home/gitpod/lldb_commands.py
